# Para el backend
cat > ~/smartfoodai/backend/railway.toml << 'EOF'
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node src/server.js"
restartPolicyType = "on_failure"
EOF
